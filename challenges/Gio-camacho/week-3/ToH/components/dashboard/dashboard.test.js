const { heroes } = require("./dashboard.component");

describe("Given an ..", () => {
  describe("When a ...", () => {
    test("Then should be...", () => {
      heroes();
      expect().toBe();
    });
  });
});
