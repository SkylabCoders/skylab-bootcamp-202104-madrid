

const princpial = require('/.ejercicioDeArray')
const array = [1,2,3]
const multiplicar = multiplicar(num)

test('it should return a new array', ()=>{
   expect(princpial.mapa(array, callbackFunction)).toContain([2, 4, 6])
})