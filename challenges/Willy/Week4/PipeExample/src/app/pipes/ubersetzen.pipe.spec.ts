import { UbersetzenPipe } from './ubersetzen.pipe';

describe('UbersetzenPipe', () => {
  it('create an instance', () => {
    const pipe = new UbersetzenPipe();
    expect(pipe).toBeTruthy();
  });
});
