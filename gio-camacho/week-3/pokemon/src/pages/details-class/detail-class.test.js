describe('Given an class', () => {
  describe('When is iterated', () => {
    test('Then shuolb be ..', () => {
      const pokemon = new Details();

      expect(pokemon).toBe({ name: undefined });
    });
  });
});
