import { CommaToDotPipe } from './comma-to-dot.pipe';

describe('CommaToDotPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaToDotPipe();
    expect(pipe).toBeTruthy();
  });
});
